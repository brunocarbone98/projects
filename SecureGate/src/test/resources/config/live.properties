# The 'live' environment is the deployed Shipping Hub web app on Railway.
# NOTE: the production deploy is a single container exposing only the Next.js WEB;
# the Express API is internal (localhost inside the container) and is NOT public.
# So the API suite (api/, bdd/) targets a LOCAL instance; 'live' is for the Phase-3
# UI suite (Selenium) against the public web app below.
apiBaseUrl=https://shipping-hub.up.railway.app
webBaseUrl=https://shipping-hub.up.railway.app

demoTrackingCode=PTY-2026-001001-0
customerEmail=ana@example.com
customerPassword=Password123!
adminEmail=admin@shippinghub.test
adminPassword=Password123!
