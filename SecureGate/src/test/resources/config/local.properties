# SecureGate target: a LOCAL Shipping Hub (../FullStackHub running on your machine).
# Bring it up with: docker compose up -d (Postgres) + pnpm --filter @shipping-hub/api db:deploy db:seed + pnpm dev
apiBaseUrl=http://localhost:4000
webBaseUrl=http://localhost:3000

# Seeded demo data (see ../FullStackHub/apps/api/prisma/seed.ts)
demoTrackingCode=PTY-2026-001001-0
customerEmail=ana@example.com
customerPassword=Password123!
adminEmail=admin@shippinghub.test
adminPassword=Password123!
